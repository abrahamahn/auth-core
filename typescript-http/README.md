# @abrahamahn/auth-http

Framework-neutral HTTP contracts and request metadata helpers for Auth Core.

The package owns bounded client metadata extraction, cookie reply contracts, and coarse device
labels. It does not mount routes, choose cookie policy, trust proxy headers by itself, or define
application authentication and authorization policy.
